export function CurrentDirLocation() {
  // Todo shows the current directory location as links separated by /
  // Links clickable an open page that user clicks
  // synced with FileManager.$currentPath
  // first part (~) replaced by button with home icon instead of link
  // can be edited and input by pressing pencil icon from right
  // user can exit from dir using button from the left (if not on top level already)
  // if path too long shows horizontal scroll (scrolled to the right end position by default)
  return null;
}
