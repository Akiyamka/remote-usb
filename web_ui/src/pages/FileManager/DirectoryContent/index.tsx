export function DirectoryContent() {
  // Todo shows the directory (FileManager.$currentPath) content as grid
  // different icons for dir and files
  // always sorted in order - directories first, then files
  // user can navigate into dirs by click
  // user can see files size
  // user can see files last modification data
  // user can open files by click (file opens in new tab)
  // user can delete files by click three dots in file row and select "delete" from menu
  return null;
}
