export function UploadFileButton() {
  // TODO shows modal window when pressed
  // User can drag and drop files to it, or press upload button inside modal
  // user can see uploading progress of every file in percentage and size
  // when uploads in progress - progress ring around button shows overall progress
  // user can cancel uploads individually
  // user can close modal but it will not stop uploading
  // and press button again to reopen modal
  return null;
}
