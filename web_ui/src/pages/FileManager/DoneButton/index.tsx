export function DoneButton() {
  // TODO:
  // switch device to storage emulation mode
  // Shows message about rejected attempt if at least one file downloaded or uploaded
  return (
    <button>
      Done
    </button>
  );
}
